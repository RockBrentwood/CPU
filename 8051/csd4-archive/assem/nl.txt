nl.c, notes on use.
━━━━━━━━━━━━━━━━━━━
0. Command Line, Input Format
─────────────────────────────
This utility will convert UNIX-style newlines into DOS-style newlines by appending every carriage return (CR) with a newline (NL).

The typical conmmand line for this utility is as follows:
	nl Input >Output
